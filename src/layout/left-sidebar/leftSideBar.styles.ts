import React from "react";

const wrapper: React.CSSProperties = {
  padding: "8px 16px 0 16px",
  backgroundColor: "#fff",
  borderRight: "1px solid #f3f3f3",
};

const fieldsetItem: React.CSSProperties = { padding: 4 };

export default { wrapper, fieldsetItem };
