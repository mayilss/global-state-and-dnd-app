import React from "react";

const wrapper: React.CSSProperties = {
  backgroundColor: "#fff",
  width: "100%",
};

const smileysWrapper: React.CSSProperties = { padding: 16 };

const submit: React.CSSProperties = { width: 200, margin: "auto" };

export default { wrapper, smileysWrapper, submit };
